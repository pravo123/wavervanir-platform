// Explicitly empty: this landing site uses hand-written CSS only,
// no Tailwind / PostCSS plugins. Prevents Vite from inheriting a
// PostCSS config from a parent directory.
module.exports = { plugins: [] };
